import { SetDividerDirective } from './set-divider.directive';

describe('SetDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new SetDividerDirective();
    expect(directive).toBeTruthy();
  });
});
