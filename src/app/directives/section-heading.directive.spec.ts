import { SectionHeadingDirective } from './section-heading.directive';

describe('SectionHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
